
{
  "name": "taklan-pm-android",
  "version": "1.0.0",
  "description": "PM Management Android Build",
  "scripts": {
    "build": "npm run build:web && npx cap sync android",
    "build:web": "echo \"No web build step defined\"",
    "android": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.0",
    "@capacitor/core": "^6.1.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}


---
